---
type: log
title: "Update Log"
description: "Chronological log of structural changes to this bundle."
---
# Update Log

## History

* **2026-08-07** — **CR-029**: the FR-052 classifier run over the ecosystem, and the two published property-shape figures **corrected** against it (report: `~/dev/reports/2026-08-07-ac-property-shape-sweep.md`, agent-ix/quire-rs#20 Phase B). Spec-only; the engine work it measures is already on the branches beneath. **What was measured:** every criterion the `ac` grammar binds to, classified by the shipped `classify_properties` engine — one call per document, the engine's own binding rather than the harness's `criteria_cells` reimplementation, which is CR-022's recorded defect and is why the census is the primary record here and no figure in it is reconstructed from a second derivation. **Over what corpus:** **197 repositories, 3,333 requirement documents, 13,950 binding criteria**, deduplicated against every duplication shape `~/dev` actually contains rather than against the skip list's own comment — top-level `worktrees/`, nested `.worktrees/`, `.claude/worktrees/`, `.git/worktrees/`, `.ticket-runner/`, and `<repo>-task<N>` siblings confirmed by `.git` being a file. Dedupe is load-bearing: the four `.worktrees/phase-b*` trees of this very stack would otherwise have counted quire-rs's own spec **five times** and pulled the ecosystem headline up by roughly two points on its own. The harness's stale `AC_ARCHETYPES` constant was also fixed to the CR-020 binding (FR/NFR/StR, not FR/NFR/US/StR/IT) — −1,115 documents (−25.1%) and −68 cells (−0.6%); the property census is unaffected and that was verified, not assumed, since `classify_properties` returns 0 records for a US and 0 for an IT document (FR-052-AC-9). **Figures corrected:** [FR-047](./functional/FR-047-acceptance-criteria-grammar.md)'s CR-013 note recorded **24.9%** (ecosystem) and **50.8%** (quire-rs) property-shaped, with a 42.5% quantified / 8.3% metamorphic split. All three are struck through and replaced with the engine-exact pair **20.6% ecosystem (2,867 of 13,950) / 52.6% quire-rs**, using CR-022's treatment — the old numbers stay visible and what they were wrong about is stated. They were wrong in *provenance*, not arithmetic: hand-classified by quantifier, over a corpus that was not deduplicated, and produced before any classifier existed, so nothing measured them with the engine that now defines the classification. Neither was reconciled toward; the gap between a spec written by this program and the rest of the ecosystem turns out to be *wider* than recorded, 52.6% against 19.5% for the ecosystem excluding quire-rs. **Predictions the corpus falsified**, both corrected in [FR-052](./functional/FR-052-acceptance-criteria-property-classification.md): (1) CR-020's caution that StR criteria "will legitimately score low on property-extractability", quoted verbatim in FR-052's Description — **StR is the highest-scoring archetype at 29.1% (325 of 1,117), above NFR at 28.9% and FR at 19.2%**, because StR `Validation Criteria` are frequently determiner-initial capability statements the determiner rule catches while FR criteria skew toward `When`/`Given` conditionals it does not. The warning is kept — a low score on any archetype is still not a quality failure and FR-052 is still not a gate (CON-1) — but it stops being presented as a description of the measurement. (2) The idiom registry's stated fallthrough, that a missed idiom "degrades a label to a less specific shape" and the cell stays `Universal` and therefore extractable — **61 of the 70 registry-attributable labels (87%) fall to `Example`, not `Universal`**; only 5 (7%) behave as described. The design's *conclusion* survives on a different mechanism than the one given: CON-4 derives `extractable` **structurally**, so the registry cannot move it, and the twin sweep (registry active vs stripped, same wheel, same corpus) confirms `extractable` differs on **0 of 13,950 criteria** — TC-790 pins this on fixtures, this is the corpus-scale confirmation. The registry's whole reach is 70 cells, **0.5% of the corpus**, and 0 extraction decisions, so growing it cannot move the headline by a single criterion and per the sweep's stop rule it will not be grown to try. `row_id` is present on **100%** of records, so the Phase C tracking-tag dependency holds corpus-wide and not just on fixtures. **Two open questions are referenced, not decided**, both recorded in FR-052's new "Open questions" section: what a consumer does with a `{property: <metamorphic>, extractable: false}` record — 63 criteria, 13.2% of the metamorphic set, carry a metamorphic label while their structure reads `Example` — filed as agent-ix/quire-rs#46; and whether the determiner rule's start-of-statement anchor should reach conditional quantification (`When`/`Given`/`If`, mid-sentence `any`/`every`), which sampling puts at ~30% recall against ~90% precision and which changes the size of Phase C's input by roughly 3× — filed as agent-ix/quire-rs#45. Deciding either from this bullet would be the tuning the stop rule prohibits. No AC added and no classifier pattern touched — the engine measured is byte-identical to the one merged. Matrix unchanged at 464/464; TC-779..791 flip 🚧 → ✅ against the implementation on this stack.

* **2026-08-07** — **CR-028**: acceptance criteria gain a **second, orthogonal shape axis** — property shape — in new [FR-052](./functional/FR-052-acceptance-criteria-property-classification.md) (agent-ix/quire-rs#20, Phase B of the umbrella agent-ix/quire-rs#17). Spec-only; no engine change lands in this slice. The program's goal is turning acceptance criteria into executable property-based tests, and CR-013 removed the route to them without replacing it: making the **assertion** canonical deleted the planned EARS→property extraction path, and nothing took its place, so no property test anywhere in the ecosystem is derived from an acceptance criterion today. The assertion is in fact the better substrate, not the worse one — a property test needs a generator domain, a precondition and an oracle, and EARS supplies only the last two (*"the system shall X"* quantifies over one system, not over a domain), whereas a quantified assertion carries all three: the determiner is the quantifier, the restrictive relative clause is the precondition, and the predicate is the oracle. FR-052 therefore classifies every criterion the `ac` grammar already binds to into a **closed** property-shape enum (`RoundTrip`, `Idempotence`, `Ordering`, `Invariant`, `Universal`, `ErrorCase`, `Lifecycle`, `Concurrency`, `Example`, `Unclassified`) under one **fixed total precedence** pinned in the spec rather than left to implementation, so structure outranks quantification and a universally quantified round-trip labels `RoundTrip`. For a `Universal` criterion it emits `{domain, precondition, oracle}` spans that are **statement-relative, never file-relative** (a supplement statement is synthesised by joining wrapped lines, so an offset does not index the file; only `line` is), and each span carries both byte offsets and its own text — offsets so the engine can assert the spans partition the statement, text because handing a UTF-16 consumer raw UTF-8 byte offsets is a defect generator, and the redundancy is checkable (FR-052-AC-6). Each record also carries `row_id` — without the criterion id the downstream generator cannot emit the tracking tag that coverage reconciliation keys on, and the chain would break silently — plus `signals`, the stable ids of what fired, which is the audit trail CR-019 asked for when it found the original sweep script had never been saved and the decision could not be re-derived. There is **no confidence float**: a float is unreviewable and invites the threshold tuning this program has already been burned by, so `Unclassified` is an explicit first-class outcome instead; and `extractable` is derived in exactly one place so the coverage column, any summary line and any downstream consumer cannot disagree about the ratio. `SchemaShaped` from the original report is **deliberately excluded** — its signal is repo state ("an OpenAPI schema exists"), not statement text, which would break determinism and corpus-comparability. Four constraints hold the boundaries: **CON-1** — classification never constructs a `GrammarFinding` and is never addressable by the FR-048 `grammar_severity` registry, so with no severity key there is no promotion path and `--strict` immunity holds by construction; a pass/fail gate on "is this criterion property-shaped" would invite authors to reword criteria to satisfy a checker instead of to describe the system, and it is the same failure mode as narrowing a check until its count looks comfortable. **CON-2** — the engine never names a test framework, harness or generator library; the shape→harness mapping belongs to the downstream `spec-correctness` work (quoin#46). **CON-3** — the shape enum is closed in the engine and extension happens by declaring idiom phrases, never by adding a shape, since a shape with no generator silently drops criteria. **CON-4** — a criterion's `extractable` value never depends on a module-declared idiom. That last one is the structural answer to whether the module `property_idioms:` registry is another open-set trap: CR-014's rule is that a closed set which *suppresses* is safe while an open set whose membership is *required* is not, which is what retired `no-observable-outcome` at ~13% sampled precision. Three of the four metamorphic classes are not actually open (invariant = English function words, idempotence = quantity adverbs, ordering = a small lexicon), and even round-trip has a closed structural signal — two operations composed plus an identity back-reference (`yields the input`, `returns the original`, `byte-identical`) — so **closed structural signals carry extraction coverage and the registry only refines labels**. A repo declaring nothing gets full reach; a missed idiom degrades a label to a less specific shape rather than dropping a criterion, and TC-790 pins that as a property test rather than an assumption. Also: [FR-047](./functional/FR-047-acceptance-criteria-grammar.md) gains a CR-028 note recording that `AcShape` is unchanged and **gains no variant**, and that the `ac` finding stream is **byte-identical** with the classifier present and absent (FR-052-AC-7), so every corpus figure in the notes above stands unmoved; [FR-050](./functional/FR-050-declarative-coverage-computation.md) gains a CR-028 note and **FR-050-AC-13** for the `criteria` field and the two new `CoverageTotals` counts, whose byte-identical guarantee under FR-050-AC-7 now covers content FR-050 does not otherwise describe. CR-020's recorded caution stands and is quoted verbatim in FR-052: StR criteria are validated by demonstration rather than quantified over an input domain, so they will legitimately score low on property-extractability and #20 must not read that as a quality failure. Adds FR-052-AC-1..13 + TC-779..791 and FR-050-AC-13 (TC-788, shared); every new TC ships 🚧 pending the engine slice; matrix 464/464.

* **2026-08-06** — **CR-022**: FR-047-CON-1 baseline sweep run and the shape-share figures in the CR-013 note **corrected** (report: `~/dev/reports/2026-08-06-ac-canonical-shape-sweep.md`, agent-ix/quire-rs#21). **No cell converted and no `grammar_severity` default changed** — both remain user-gated, and the sweep found the job is about **seven times** the scope it was scoped against. The CR-013 note recorded `obligation` 2.9% / `given-when-then` 1.3% and concluded non-canonical shapes were "~4.2% of the corpus (~506 cells, concentrated in 17 repos)". Those figures never agreed with the primary measurement: `non-canonical-shape` fires on exactly `obligation | given-when-then`, and the fit report of the same date and corpus records it at 2,047 findings = 17.2% of cells — 2.9% + 1.3% cannot produce 17.2%. The distribution table was the wrong record; a check's own finding count is a census, not a sample. Same class of defect as the CR-014 mismatch reconciled in agent-ix/quire-rs#24. Re-measured over a deduplicated 192-repo / 16,449-cell corpus (worktree clones excluded): `assertion` 78.5%, **`obligation` 19.3%**, `given-when-then` 1.7%, `unstructured` 0.5% — a conversion target of **3,458 cells across 50 repos**, extremely top-heavy (`ecaz` alone 2,413; six repos hold 80%). Sampling confirms `non-canonical-shape` itself is sound at ~95% precision and, applying the CR-017 lesson, none of the sampled findings is a checker defect — the cells really are obligations. Promotion proposal presented for sign-off: `ac:vacuous-outcome` (31, 0.19%) and `ac:non-singular` (121, 0.74%) → `error`; `ac:unclassifiable` (82) stays `warning` pending a post-CR-019 precision re-sample; `ac:vague-response` (101) stays `warning` as it depends on glossary coverage; `ac:non-canonical-shape` (3,458, 21.0%) stays `warning` globally with per-repo promotion as each repo is swept clean — enforcement following normalization, the same sequence gating spec-artifacts-process#12 and spec-artifacts-iso#9. Reversing CR-013 for `obligation` on the strength of its 19.3% share is recorded as explicitly declined: FR-047-CON-2 forbids exactly that, and re-deriving canon from corpus frequency is the failure mode the program warns about. Task-009's last box stays open until sign-off. Doc-only; matrix unchanged at 446/446.

* **2026-08-06** — **CR-021**: the open question CR-017 left — whether `ears` adopts the mention/use mask — is **closed as "not yet", with the reason recorded** ([FR-042](./functional/FR-042-requirement-grammar-check.md) Non-goals + CR-021 note). No behaviour change ships. The mask was implemented at the four `ears` sites that read keywords (pattern classification, `shall` counting, subject detection, trigger detection) and measured over the same corpus as CR-020 (36,584 cells, 197 repos) with `scripts/ac_corpus_sweep.py`. Masking those alone moved 4 findings and **raised `unclassifiable` 448 → 450** — the tell that masking classification without masking *segmentation* trades one false positive for another: a sentence is selected as a statement because it contains a `shall`, then judged unclassifiable because that `shall` was quoted. Masking the segmentation gate too and reusing `ac`'s code-span-aware `split_sentences` removes the regression and nets −3 `non-singular`, but reading those three shows **only one is a mention/use fix**; the other two (ix-flow FR-009, quoin FR-019) are genuine two-obligation statements being silenced. The cause is not the mask but that `ears` segmentation is **line-based**: when a code span wraps across source lines the next line begins inside an unterminated span, so code-span-aware splitting reads the rest of that line as quoted and the mask neutralizes real modals in it. `ac` never hits this — its statements are table cells and supplement bodies, not wrapped prose lines. One fix against two regressions is a net loss, so the mask is declined **for now**; this is not a finding that the distinction is wrong for `ears` (the two grammars disagreeing is a real inconsistency), but that it cannot land safely on line-based segmentation. Adoption is conditional on the already-planned paragraph-joining refinement — re-run this measurement then. No AC or TC changes; matrix unchanged at 446/446.

* **2026-08-06** — **CR-020**: the `ac` binding follows the **module contract**, not a corpus census. CR-014 widened it from `FR` to FR/NFR/US/StR/IT on cell counts (FR 11,476 · NFR 340 · US 69 · StR 20 · IT 14), but a census measures what authors wrote: checking `spec-artifacts-iso`, only **FR** declared an acceptance-criteria table — NFR's section was optional free prose, StR has no `Acceptance Criteria` section at all (its required section is `## Validation Criteria`), and a US carries `## Acceptance Examples (Illustrative)` whose skeleton states outright that nothing in it is binding. Twenty StR cells across 199 repos are twenty improvised tables. That caused two failures in opposite directions — reading US criteria as binding treats discovery context as verification criteria and inflates every number counting them, while missing StR's required validation criteria hides binding criteria that genuinely exist. CR-014's premise that "the criteria of a user story … are verification statements of the same kind" is **wrong for US** and is retracted. The binding is now per-archetype section + sub-id kind, following [spec-artifacts-iso#9](https://github.com/agent-ix/spec-artifacts-iso/issues/9) which gives StR and NFR real tables: **FR/NFR** under `Acceptance Criteria` with `-AC-` supplements, **StR** under `Validation Criteria` with `-VC-` supplements (ISO/IEC/IEEE 29148 separates validation from verification at the artifact level; only the table *shape* is unified), **US and IT unbound**. Two engine sites follow: `src/corpus/unlinked.rs` learns `-VC-` in **both** `parent_id` and the token regex — the second was not in the plan and was caught by the test, since matching only `StR-001` would link the parent and leave a dangling `-VC-2`; and a new `forbidden_section` lint rule type gives the module a way to flag an `## Acceptance Criteria` heading on a US, which `section_body_pattern` cannot express because a missing section is defined to produce no finding. **Baseline restated** (every count since CR-014 mixed conformant FR cells with improvised non-FR ones), same 36,582 cells via `scripts/ac_corpus_sweep.py`: **16,792 → 15,247 findings (−9.2%)** — `non-canonical-shape` 6,859 → 5,958, `vague-response` 315 → 286, `unclassifiable` 184 → 157, `vacuous-outcome` 51 → 40; `ears` unchanged, as expected. Note for downstream: StR criteria are validated by demonstration rather than quantified over an input domain, so they will legitimately score low on property-extractability — the property-shape classifier (#20) must not read that as a quality failure. Amends FR-047 Description + AC-6; adds FR-036-AC-7 + TC-764 and FR-039-AC-11 + TC-765; matrix 446/446.

* **2026-08-06** — **CR-019**: both of CR-014's high-volume checks re-measured against the open questions on agent-ix/quire-rs#18 and #19, over **36,580 `Criteria` cells in 197 repos** (report: `~/dev/reports/2026-08-06-ac-check-remeasure.md`). The original run's sweep script was never committed, so the decision could not be re-derived; the harness is now `scripts/ac_corpus_sweep.py`. **(1) CR-014 stands.** `no-observable-outcome` was restored behind a measurement-only `remeasure` cargo feature — never enabled in a shipping build — with all three mechanical defects fixed and the 73-verb widening the fit report tested now adopted (the built-in observable set is 86 verbs). Repaired, it fires on **14.8%** of cells against 51.0% originally, a 3.4× drop; but sampled precision **fell** to ~13% (4 of 30), because the mechanical fixes removed the easy true positives and the residual is almost entirely the open verb space — `invalidates`, `clears`, `revokes`, `displays`, `exchanges`, `recovers`, `activates`, `delegates` are legitimate outcomes absent from any finite list. A 30-cell recall frame found **0** untestable cells unflagged. The check does not return, not even shipping `off`: a check nobody could defensibly enable is not worth the surface. **(2) `unclassifiable` is documented honestly rather than renamed.** Its predicate test is satisfied by **99.6%** of cells and **23.4% via the `\b\w+(s|ed|ing)\b` branch alone**, which matches any plural noun. Tightening it to require a finite verb needs part-of-speech tagging, out of scope for a deterministic engine, so FR-047 now describes it as a **bare-fragment heuristic** and retracts the reading of CR-014's 5,187 → 87 drop as a precision improvement. **(3) The false-positive class both issues required fixing is fixed.** A predication whose copula is elided — an existential/quantifier head (`No refresh_token in response body`, `Only one active password_reset token per user`) or a predicative adjective (`Lockout response identical to normal failure`) — is now a predicate. This removed **92 of 276** `unclassifiable` findings (−33%), **every one a false positive**, and closes the one residual the original fit report called unfixable without POS tagging. Like `vacuous_predicates` it is a *closed* set that *suppresses* a finding rather than an open one whose membership is required. A `-able`/`-ible` branch was tested and rejected: 2 true fixes against 1 regression (it rescues the placeholder `[observable outcome]`). No `grammar_severity` default changed — promotion stays user-gated by FR-047-CON-1. Adds FR-047-AC-14 + TC-763; matrix 444/444.

* **2026-08-05** — **CR-018**: module discovery is sorted, closing a determinism hole under every first-wins merge. `load_modules` iterated `std::fs::read_dir` directly, whose order is unspecified, so which module won an `archetypes` / `lexicon` / `edge_types` / `grammar_severity` / `traceability` collision depended on filesystem layout. Three merge tests (TC-637, TC-668, TC-717) passed locally and failed on the CI runner for exactly that reason — the hole is invisible to a single-machine run, which is why it survived since FR-013. Discovery now sorts by path. Adds [NFR-006](./non-functional/NFR-006-determinism.md)-AC-5 + TC-762; matrix 443/443.

* **2026-08-05** — **CR-017**: the `ac` grammar stops confusing **mention with use**. A criterion that *quotes* a keyword as example data (``a statement with two `shall` clauses yields exactly one `non-singular` finding``) was classified as though it imposed an obligation — which accounted for every one of the seven `non-canonical-shape` findings standing against this repo's own spec, since FR-042/FR-043/FR-047 describe a grammar and therefore quote its keywords. Shape classification and obligation counting now read a copy in which each closed code span's contents are masked; the signal and lexicon checks still read the real words, so backticked identifiers and lexicon terms keep their suppressing effect. Masking exposed a second defect it would have made worse: supplement prose was segmented on `". "` without regard to code spans, cutting embedded examples (``` `EXPLAIN … SELECT ... ORDER BY col LIMIT 10` ```) in half and leaving unbalanced backticks — segmentation is now code-span aware, which is what makes the mask safe. Measured over the same 199-repo corpus as CR-014: **3,956 → 3,949** findings — 16 mention-only false positives removed, 9 true positives restored that the mask alone had hidden, 1 further true positive surfaced, and **no new finding in any other check**; this repo's own `ac` findings fall 10 → 2 (both remaining are `vague-response` on PyO3-parity criteria). Whether `ears` should adopt the same distinction is left open. Adds FR-047-AC-13 + TC-761; matrix 442/442.

* **2026-08-05** — **CR-015**: the traceability model becomes the single source for matrix column vocabularies, after the FR-003 ecosystem sweep found them declared in two places and drifting. [FR-050](./functional/FR-050-declarative-coverage-computation.md) gains: a `retired` status class and **leading-marker** classification, so `✅ Complete` and `⚠️ scale evidence deferred` class correctly while keeping the note that says why; a `vocabularies:` block (first entry `test_type`) that both a module's matrix contract and the rollup read; and two **default-off** reference normalizations, `expand_ranges` (`FR-001..FR-006` → concrete ids) and `strip_annotations` (a parenthetical qualifier contributes no reference). Sub-id kinds stay derived from the declared trace targets rather than enumerated — nothing is code-generated. Adds FR-050-AC-10..12 + TC-758..760; matrix 441/441.

* **2026-08-04** — **CR-014**: FR-047's two high-volume checks rebuilt after an ecosystem fit check (report: `~/dev/reports/2026-08-04-ac-grammar-fit.md`) ran the grammar over 5,027 requirement documents in 199 repos / 11,919 acceptance-criteria cells. `no-observable-outcome` fired on 51% of cells at ~35% sampled precision and `unclassifiable` on 43.5% at ~12%; the cause is structural — observability lives in an open-ended verb space (1,201 stems; the built-in 13 covered 14.5%), and declaring 73 corpus-mined verbs cut findings 60% but left precision unusable. The checks are therefore inverted: **`no-observable-outcome` becomes `vacuous-outcome`**, detecting a closed set of vacuous predicates (module-extensible via a new `vacuous_predicates` registry) suppressed by any concrete signal, lexicon term, or declared observable verb; **`unclassifiable` becomes structural** — no predicate at all. Measured over the same corpus, against 11,269 findings from the two retired predicates before the change: 25 and 39 (0.21% / 0.33%) under the FR-only binding as measured mid-change, and **32 and 87 as shipped** once the binding widened in this same CR — the shipped figures, and the ones `~/dev/reports/2026-08-04-ac-grammar-fit.md` and CR-017's 3,956 → 3,949 total are stated against. The same evidence settled the **binding**: `ac` now covers every requirement archetype carrying an `Acceptance Criteria` table (FR/NFR/US/StR/IT), since FR-only reached 76.9% of AC-bearing documents and US/NFR criteria are the same shape. `observable_verbs` keeps its ADR-0009 role as the suppressor vocabulary. Also fixes three mechanical defects the check exposed: irregular/`-y` verb inflections, obligation clause-slicing dropping pre-modal concrete signals, and supplement-prose segmentation producing truncated fragments. Bullet-form AC sections remain unsegmented — recorded as future work.

* **2026-08-04** — **CR-013**: [FR-047](./functional/FR-047-acceptance-criteria-grammar.md) canonical acceptance-criteria shape changed from **EARS to the assertion**. The Gate G1 baseline over this repo's spec produced 340 `ac` findings across 44 FR documents, 322 of them `unclassifiable` — each a correct report that a declarative assertion is not an obligation, and only 18 substantive. Classified by quantifier instead of style, 50.8% of the corpus's 327 acceptance criteria are already property-shaped. An AC is a verification statement, not an obligation, so the canonical shape is the one that carries the test oracle. `unclassifiable` now means *structureless* (no modal, no Given/When/Then, no observable signal); `non-canonical-shape` now fires on obligation-shaped cells as well as GWT; shapes are `assertion | obligation | given-when-then | unstructured`. New **FR-047-CON-2** forbids making the canonical shape configurable — FR-048's per-check severity map is the opt-out. [FR-042](./functional/FR-042-requirement-grammar-check.md)'s non-goals bullet reworded accordingly (EARS remains the grammar for normative statements). Matrix: TC-707 and TC-751 rewritten; AC count unchanged at 438.

* **2026-08-04** — SR-002 finding resolution (user-authorized, incl. pre-existing artifacts). FND-002: [FR-045](./functional/FR-045-filament-core-extraction-engine.md) CON-4 reworded via **CR-012** — the `ix://` org segment is caller-supplied (`FilamentExtractionInput.org`, no engine default), aligning the constraint with PR #15's shipped `normalize_ref` behavior. FND-003: [FR-042](./functional/FR-042-requirement-grammar-check.md) "Non-goals (v1)" minimally reworded — the AC grammar is realized by [FR-047](./functional/FR-047-acceptance-criteria-grammar.md) with EARS (not GWT) as the canonical AC shape. FND-005: [FR-047](./functional/FR-047-acceptance-criteria-grammar.md) `no-observable-outcome` (retired by CR-014, below → `vacuous-outcome`) vocabulary reworked to module data per ADR 0009 — engine built-in defaults + module `observable_verbs` registry, first-wins merge (FR-043 `lexicon` pattern); added FR-047-AC-12 + TC-757. FND-004: external deliverables tracked as EXT-1..4c entries in Plan-001 (owning repos + gated tasks). Matrix: 438/438 ACs at 100%.
* **2026-06-15** — Adopted OKF-compatible bundle structure with directory indexes.
* **2026-06-16** — Added [FR-037](./functional/FR-037-base-concept-schema.md) (base concept frontmatter schema: required non-empty `type` + optional typed `description`/`tags`; `validate_base_concept` / `validate_concept_shape`) and [FR-038](./functional/FR-038-okf-bundle-validation.md) (OKF bundle validation: Strict vs Okf postures, untyped-corpus-doc-is-error, index completeness + root `okf_version`). Added a CR note to [FR-027](./functional/FR-027-whole-spec-query-api.md) clarifying the bundle validator promotes the corpus-level non-fatal `UntypedArtifact` diagnostic to a hard error. Matrix: TC-590..596, TC-600..607 at 100% AC→TC coverage (309 ACs).
* **2026-06-16** — Extended [FR-032](./functional/FR-032-validate-document.md) with composed type+object validation (CR note + AC-11..13): `validate_document_in_registry(registry, archetype, doc_text)` validates BOTH the `type` archetype AND the frontmatter `object:` archetype; resolved-object `body_extraction` failures merge into `errors`, an unknown `object:` is a `unknown-object-type` **warning**, and `ValidationResult` gains a typed `warnings: Vec<ValidationWarning>` field. The 2-arg `validate_document` stays the type-only path. Matrix: TC-610..613 at 100% AC→TC coverage (314 ACs).
* **2026-06-17** — Internal-links slice (ADR 0007: internal references are relative-path links, `ix://` is external-only). Added ADR 0007. Extended [FR-026](./functional/FR-026-intra-spec-reference-resolution.md) with a third edge-stub source — relative-path body links resolved via a path→id index — plus index/log source-exclusion and cross-source dedup parity (CR note + AC-9..11). Added [FR-039](./functional/FR-039-unlinked-reference-detection.md) (unlinked-reference detection & autofix suggestions): `unlinked_references(spec) -> Vec<UnlinkedReference>` classifies bare artifact-id tokens into AutoFix / WarnOnly / Ignore, carrying the exact relative-path link to splice; bare prose codes are never harvested directly. Matrix: TC-620..631 at 100% AC→TC coverage (326 ACs).
* **2026-08-04** — AC-grammar / traceability-coverage slice (spec-only). Added [US-017](./usecase/US-017-agent-verifies-coverage.md) (agent verifies coverage deterministically), [FR-047](./functional/FR-047-acceptance-criteria-grammar.md) (`ac` grammar on the FR-042 framework: EARS is the canonical AC shape, GWT recognized but steered via a `non-canonical-shape` advisory; non-singular / vague-response / no-observable-outcome (the last retired by CR-014, below → `vacuous-outcome`) checks; generic `[<grammar>:<check>]` `--summary`; CON-1 gates error-promotion behind a corpus baseline sweep + user sign-off), [FR-048](./functional/FR-048-per-check-grammar-severity.md) (per-check `grammar_severity` registry over `off`|`warning`|`error` + `--severity` CLI override — the configuration mechanism FR-042 left open; `off` fully suppresses a check), [FR-049](./functional/FR-049-verification-reference-integrity.md) (model-driven `dangling-trace-reference` bundle check), [FR-050](./functional/FR-050-declarative-coverage-computation.md) (declarative `traceability:` manifest model + generic `quire coverage` rollup; verdict policy stays in quoin), [FR-051](./functional/FR-051-source-symbol-extraction.md) (deterministic source-symbol extractor with FR-045-aligned graph records; framework-native markers — pytest `trace` marker, Rust `#[trace]` no-op attribute, TS `trace()` helper, all module-declared — are the canonical statically-parsed trace form, textual tags are a legacy class whose recognition drops after a user-gated normalization sweep, CON-3; per-language marker support packages are separate deliverables outside quire-rs core), and ADR 0010 (proposed: SMT-backed cross-requirement consistency analysis — open questions only). Matrix: TC-707..753 planned at 100% AC→TC coverage (435 ACs). Backfilled FR-045/FR-046/US-016 index entries.
* **2026-06-17** — FR-039 hardening (rollout fix): a single inline-code span holding **>1** artifact token (e.g. `` `FR-012/FR-013` ``) is now **skipped** (Ignore), not converted — emitting one whole-span fix per token produced overlapping byte spans that corrupted on apply during the ecosystem rollout. Added FR-039-AC-10 + TC-632 (327 ACs). quire-cli `fix` also gained a defensive overlapping-span guard on apply.
