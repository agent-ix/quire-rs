# FR-054 fixture (TC-847): an entry carrying an unknown key. `deny_unknown_fields`
# rejects it at load, so a typo cannot be silently discarded — the house rule
# that makes engine-before-module ordering load-bearing.
name: catalog-bad-key
version: 0.1.0
description: Catalog entry with an unknown key (FR-054 fixture)
artifact_types:
- name: FR
  defaults:
    id_pattern: FR-{next:03d}
verification_catalog:
  mutation-testing:
    name: Mutation testing
    class: Test
    definition: Seed faults and measure whether the suite detects them.
    evidence_knid: mutation-score
