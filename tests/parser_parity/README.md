# parser_parity

Parser-parity corpus comparing quire-rs against the TypeScript and Python
reference implementations. Populated by Task 008 / G1 (parser parity gate)
in `plan/plan.md`.

Empty for now; the directory exists so subsequent tasks can drop fixtures
in without separate setup commits.
