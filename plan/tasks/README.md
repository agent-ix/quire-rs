# quire-rs Task Index

Generated by `/spec-to-plan` from `~/dev/quire-rs/spec/`. See `../plan.md` for the full plan, DAG, and gates.

## Status legend

- `not started` — ready to start (no blockers)
- `blocked on X` — waiting for task or gate
- `in progress` — claimed by an agent
- `complete` — merged + verified

## Active Tracks

### Track A — Critical Path (serial)

| # | Task | Status | Gate |
|---|---|---|---|
| 001 | Parser primitives (FR-006..009) | not started | — |
| 002 | parse_document API (FR-005) | blocked on 001 | — |
| 003 | Query API (FR-010) | blocked on 002 | — |
| 004 | Parser parity gate | blocked on 003 | **G1** |
| 005 | Archetype loader (FR-013) | blocked on G1 | — |
| 006 | Module activation (FR-014) | blocked on 005 | — |
| 007 | Strict MiniJinja env (FR-004) | blocked on 005 | — |
| 008 | schema_for (FR-003) | blocked on 005 | — |
| 009 | Schema validation (FR-002) | blocked on 005 | — |
| 010 | Render dispatch (FR-001) | blocked on 006, 007, 008, 009 | — |
| 011 | Parity harness (FR-012 partial) | blocked on 010 | — |
| 012 | Render parity gate (FR archetype) | blocked on 011 | **G2** |
| 013 | Full parity sweep (17 archetypes) | blocked on G2 | — |
| 014 | Perf gates (NFR-001, 002, 007) | blocked on 013 | **G3** |

### Track B — Parallel (start anytime)

| # | Task | Status |
|---|---|---|
| 019 | QuireError shape (NFR-005) | not started |
| 020 | Determinism harness (NFR-006) | not started |
| 021 | Scaffold polish (StR-004) | not started |

### Track C — Post-G2 (extract side)

| # | Task | Status |
|---|---|---|
| 015 | DSL locators + single-yield (FR-011.1) | blocked on G2 + 003 |
| 016 | DSL multi-yield + emit_edges (FR-011.2) | blocked on 015 |
| 017 | Relationship harvesting (FR-015) | blocked on 016 |
| 018 | Fallback locators (FR-016) | blocked on 015 |

## Quality Gates

| Gate | After | Measures | Pass criteria |
|---|---|---|---|
| **G1** | 004 | Parser correctness vs TS/Py reference | TC-020 + TC-021 100% pass |
| **G2** | 012 | Render parity for FR archetype | TC-030 for FR byte-exact |
| **G3** | 014 | Perf NFRs (render + parse + load) | NFR-001 + NFR-002 + NFR-007 baselines set |

## Coordination Rules

1. **Do not skip gates.** Gate failure means stop — diagnose before adding downstream work.
2. **Critical-path tasks merge serially.** Track B and Track C can merge as they complete (after their gates).
3. **Track C is BLOCKED until G2 passes.** No DSL work before render parity is proved.
4. **Update this README on each transition.** Status drift breaks coordination.
5. **Each task starts with the failing test set** (TDD red), then implementation (green), then refactor + benches.
6. **Spec changes during implementation** require a CR — do not silently edit `~/dev/quire-rs/spec/` from a task branch.
7. **NFR-003 (zero unsafe), NFR-004 (license), NFR-005 (error shape), NFR-006 (determinism)** are cross-cutting — every task is responsible for not breaking them.

## Test mapping summary

- Track A produces 47 of 104 TCs (parser + loader + render + parity + perf gates).
- Track B produces 14 TCs (error shape + determinism + scaffold).
- Track C produces 25 TCs (DSL + edges + fallback).
- Remaining 18 TCs are split across infrastructure (corpus.yaml exists, CI workflow audit, dependency audits) — handled by Task 021 + the gates.
